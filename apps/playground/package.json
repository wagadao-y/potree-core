{
  "name": "playground",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter potree-core build && vite",
    "build": "pnpm --filter potree-core build && tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "potree-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/three": "^0.152.1",
    "three": "^0.154.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.9"
  }
}
